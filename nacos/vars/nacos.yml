---
# nacos version
nacos_version: 2.0.3

# nacos user
user: "root"
group: "root"

# nacos service address

nacos_address:
  - 172.27.80.84:8848
  - 172.27.80.20:8848
  - 172.27.80.106:8848


# mysql address

mysql:
  address: 172.27.80.126:3306
  user:
  password:
  database:

# nacos data path
#data_dir: nacos_storage
#nacos_log_path: "{{install_dir}}/nacos_access_log"

# nacos port
nacos_port: 8848

firewall_ports:
  - "{{ leader_port }}"
  - "{{ vote_port }}"
  - "{{ client_port }}"
  - "{{ jmx_port }}"
  - "{{ random_port }}"

# env path
install_dir: "/data"
download_path: "/root"               # your local download path
tmp_path: "/tmp"

# host group


